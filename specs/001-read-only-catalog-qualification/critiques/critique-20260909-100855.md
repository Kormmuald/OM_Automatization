# Critique Report: безопасное чтение и допуск каталога BPMSoft

> **Историческая оговорка (2026-09-13):** выводы этого отчёта о необходимости
> `AuthorizationReference` заменены текущим решением в `spec.md`: ручной запуск CLI
> оператором либо прямая текущая просьба пользователя достаточны для live read-only test.
> Сам отчёт сохраняется как снимок состояния на дату Critique.

**Дата**: 2026-09-09 10:08:55 +03:00
**Feature**: [spec.md](../spec.md)
**Plan**: [plan.md](../plan.md)
**Проверенные дополнительные артефакты**: [test-plan.md](../test-plan.md), [implementation-slices.md](../implementation-slices.md), [handoff-slices-to-tasks.md](../handoff-slices-to-tasks.md)
**Вердикт**: ⚠️ PROCEED WITH UPDATES

> **Статус-апдейт 2026-09-09:** G1 закрыт последующим явным решением человека,
> зафиксированным в `implementation-slices.md`: S01–S03 утверждены для exact target
> `specs/001-read-only-catalog-qualification`. `tasks.md` создан и прошёл format
> validation. Этот апдейт не является evidence реализации, verification или live authorization.

---

## Executive Summary

Повторная проверка учитывает уточнённый 2026-09-09 условный ручной путь: после успешных offline checks агент запрашивает отдельное решение владельца для одного target/read scope, а отсутствие ответа, отказ или отсутствие `AuthorizationReference` не запускают credential prompt или HTTP send. `spec.md`, `plan.md`, `test-plan.md` и slices согласованно сохраняют offline-first границу, двухпроходную qualification без Pass C/retry и запрет Excel/compare/Apply/browser/Git. Первоначальный process blocker G1 закрыт последующим явным human approval S01–S03; это не разрешает implementation, live BPMSoft, Write/Manage или какие-либо запрещённые действия.

## Product Lens Findings 🎯

### Problem Validation
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P1 | 💡 | Метрики полезности будущего authorised pilot не определены. | После отдельного разрешённого live run зафиксировать только безопасные агрегаты: время до decision point, число named blockers и ручных уточнений. |

### User Value Assessment
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P2 | — | S01→S02→S03 создают раннюю ценность без подмены `HUMAN_REVIEW_REQUIRED` разрешением на Apply. | Сохранить этот порядок и human decision point. |

### Alternative Approaches
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P3 | — | Port-by-copy legacy и retry-until-equal по-прежнему конфликтуют с safety boundary. | Сохранять disposition `reuse-semantics|rewrite|drop|defer` и запрет автоматических повторов. |

### Edge Cases & UX
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P4 | — | Отказ, нет ответа и отсутствие `AuthorizationReference` теперь явно имеют safe outcome без prompt/send; target change terminal. | В tasks закрепить отдельные негативные checks диагностики, prompt count `0` и HTTP send count `0`. |

### Success Measurement
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| P5 | 💡 | FR-010 требует прогнозируемый workbook scale, но именованный контракт `WorkbookScaleForecast/v1` ещё не описан в plan/research. | В пределах S03 определить и протестировать `WorkbookScaleForecast/v1` как diagnostic-only: детерминированные bucket/status для неполных данных, без Excel I/O и без трактовки как full-catalog acceptance. |

## Engineering Lens 🔬

### Architecture Soundness
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E1 | — | Domain/Application/adapters/CLI и fake transport согласованы с constitution. | Добавить architecture test до adapter implementation и запретить runtime dependency на `preparation/*`. |

### Failure Mode Analysis
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E2 | — | `TARGET_STATE_CHANGED_DURING_QUALIFICATION` terminal и не допускает Pass C, automatic new double pass или retry loop. | В каждой qualification task явно проверять exact-two-pass и отсутствие in-process retry. |

### Security & Privacy
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E3 | — | Allow-by-schema evidence и pre-write scanner сохраняют fail-closed boundary. | Для каждого нового evidence subtype требовать field allowlist и negative canary до durable write. |

### Performance & Scalability
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E4 | 💡 | Числовые limits намеренно не выдуманы; без формального forecast contract S03 может остаться неоднозначным. | Связать P5 с fixture vectors и configurable limits, не превращая bounded fixture в доказательство масштаба full catalog. |

### Testing Strategy
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E5 | 💡 | `AuditMetadataTests.cs` и `HandoffPackageTests.cs` детализированы в slices, но не перечислены отдельными строками основной matrix `test-plan.md`. | Сделать их обязательными явными task entries с трассировкой FR-013/FR-016/SC-007; не расширять scope. |

### Operational Readiness
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E6 | 🤔 | Формат, источник и offline-verifiable rule для будущего `AuthorizationReference` не определены и не могут быть выдуманы реализацией. | До отдельной live activation decision оставлять `FULL_CATALOG_NOT_QUALIFIED`; conditional manual task должна только запросить owner decision. |

### Dependencies & Integration
| ID | Severity | Finding | Suggestion |
|----|----------|---------|------------|
| E7 | — | BCL, fake transport и sanitized fixtures исключают external/live dependencies из S01–S03. | Не добавлять сторонние transport/Excel dependencies без отдельного Plan review. |

## Cross-Lens Insights 🔗

| ID | Finding | Product Impact | Engineering Impact | Suggestion |
|----|---------|---------------|--------------------|------------|
| X1 | Условный manual live path создаёт понятный следующий шаг, не расширяя offline scope. | Оператор видит безопасную причину и запрашивает только точное решение владельца. | `AuthorizationGateTests.cs` предотвращает credential prompt и HTTP send до positive reference. | Сохранить request-only task после offline verification. |
| X2 | Последующее явное human approval закрыло первоначальный blocker по slices. | Утверждён только scope S01–S03 для exact target; это не acceptance результата. | `tasks.md` может быть результатом утверждённой декомпозиции, но не доказательством реализации. | Сохранять approval record и все safety gates в задачах. |

## Findings Summary

| Metric | Count |
|--------|-------|
| 🎯 Must-Address | 0 |
| 💡 Recommendations | 3 |
| 🤔 Questions | 1 |
| Product findings | 5 |
| Engineering findings | 7 |
| Cross-lens findings | 2 |

## Consolidated Findings Table

| ID | Lens | Severity | Category | Finding | Suggestion |
|----|------|----------|----------|---------|------------|
| G1 | Cross-lens | — (closed) | Process / Slice governance | Последующее явное human approval в `implementation-slices.md` утвердило S01–S03 для exact target; `tasks.md` создан после этого решения. | Сохранять record approval; не считать его evidence реализации или live authorization. |
| P5 | Product | 💡 | Success Measurement | `WorkbookScaleForecast/v1` is not yet a named contract. | Define/test it as a safe S03 diagnostic without Excel I/O. |
| E4 | Engineering | 💡 | Performance & Scalability | Forecast inputs/buckets are not yet formalized. | Add deterministic fixture vectors and safe unavailable status. |
| E5 | Engineering | 💡 | Testing Strategy | Audit/handoff test paths are only slice-level entries. | Retain explicit tasks for `AuditMetadataTests.cs` and `HandoffPackageTests.cs`. |
| E6 | Engineering | 🤔 | Operational Readiness | Authorization reference trust rule is an owner decision. | Do not infer it; preserve the gate. |

## Recommended Actions

### Закрытый process blocker
1. **G1 закрыт**: владелец явно утвердил `specs/001-read-only-catalog-qualification/implementation-slices.md` (S01–S03) для текущего exact target; `tasks.md` создан. Решение не является разрешением реализации, live run или Write/Manage.

### 💡 Recommendations (Strongly Suggested)
1. **P5/E4**: в S03 зафиксировать и покрыть `WorkbookScaleForecast/v1` как safe diagnostic без Excel I/O.
2. **E5**: включить отдельные tasks для `AuditMetadataTests.cs` и `HandoffPackageTests.cs`.
3. **P1**: после отдельно authorised pilot определить безопасный baseline полезности.

### 🤔 Questions (Need Stakeholder Input)
1. **E6**: какой формат, источник и правило проверки будет иметь `AuthorizationReference` для будущего live read-only run?

## Preserved gates

- `FULL_CATALOG_NOT_QUALIFIED` остаётся открытым до conditional manual run и human review.
- `INDEX_SYNC_UNRESOLVED` остаётся открытым; index mutation запрещён.
- Write/Manage отсутствуют; `WRITE_ALLOWLIST_UNAPPROVED` и `WRITE_SEMANTICS_UNPROVEN` не обходятся.
- После `TARGET_STATE_CHANGED_DURING_QUALIFICATION` запрещены Pass C, retry и automatic new double pass.
- Offline fixtures/fake transport не дают live authorization; отказ, нет ответа или отсутствие `AuthorizationReference` дают zero terminal credential prompts и zero HTTP sends.

*Generated by `/speckit.critique` — dual-lens strategic and technical review for spec-driven development.*
