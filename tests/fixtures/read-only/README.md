# Sanitized read-only fixtures

Fixtures contain only synthetic identifiers and structural metadata. They must never contain credentials, cookies, CSRF values, login bodies, raw lookup values, or real target authorization.
