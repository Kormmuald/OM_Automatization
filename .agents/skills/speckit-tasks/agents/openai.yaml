interface:
  display_name: "SpecKit Tasks"
  short_description: "Создаёт упорядоченный список задач feature"
  default_prompt: "Use $speckit-tasks to generate dependency-ordered feature tasks."

policy:
  allow_implicit_invocation: false
