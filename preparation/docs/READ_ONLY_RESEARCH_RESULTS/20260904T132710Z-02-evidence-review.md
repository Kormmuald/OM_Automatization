# Independent G2 evidence review — 20260904T132710Z

**Status:** COMPLETE  
**Evidence root:** `C:\CodingAgents\codex\projects\OM_Automatization\preparation\PrototypeReadOnlyPull\bin\Release\net10.0\probe-output\20260904T132710Z`  
**Reviewer scope:** offline, read-only inspection only; no BPMSoft/login request was made.

## Known inputs

- Updated reviewer prompt: `docs\READ_ONLY_RESEARCH_PROMPTS\02-evidence-reviewer.md`.
- Current source for source-to-claim traceability: `PrototypeReadOnlyPull\Program.cs` and the locally pinned assemblies referenced by `request-contract.json`.
- Owner authorized launch despite the recorded usage-gate `PAUSE` snapshot (9% remaining; threshold 20%; reset 2026-09-04 19:38:02 MSK; primary 300-minute window).
- Resume gate snapshot supplied by the orchestrator: `PASS`, 99% remaining, same unique primary 300-minute window, reset 2026-09-04 21:51:41 MSK. This reviewer did not alter either snapshot.

## Review groups

1. Inventory and exact-byte integrity of the claimed 23-file evidence set. **Completed:** directory inventory contains exactly 23 regular files, all timestamped 2026-09-04 13:27:10 UTC. All are strict UTF-8 without BOM; every JSON file parses. The exact-byte hash manifest below records the reviewed state, but cannot prove provenance before this review or prevent later external alteration.
2. Release assembly hashes/version and request-contract / IL-backed metadata. **Completed:** independently recomputed hashes and both file/assembly versions: `BPMSoft.Common.dll` = `1.8.0.14107`, SHA-256 `42bed5e551e01e63c35d5439ed71b6335b34feec740185f6a09af8a11228ae20`; `BPMSoft.Nui.ServiceModel.dll` = `1.8.0.14107`, SHA-256 `3c147aaa90d16a6b4d3fbb3b7773876a741ca2e73f41bb690997c241e057d948`. They match `request-contract.json`. Reflection/IL inspection confirms `OrderDirection` enum `{None=0, Ascending=1, Descending=2}`, `SelectQueryColumn.OrderDirection` type, converter `ReadJson` token `0x06000809`, and `EsqBuilder.Build` token `0x06000903`. Its IL reads `SelectQuery.get_AllColumns`, conditionally calls `SelectAllColumns` (`0x060008d8`), then reads `Columns` and calls `AddQueryColumns` (`0x060008d3`); the `brfalse.s` at IL `0x00df` skips `SelectAllColumns`. Contract payload has `allColumns=false`, and exactly `Id` for `ActivityPriority`, `Id` plus unsorted `SysEntitySchemaUId` for `Lookup`. This proves local assembly semantics for the documented payload form, not successful live order/pagination by itself.
3. Six ID files: encoding, UUID-only form, SHA-256, equality and reverse comparisons. **Completed:** all six are strict UTF-8 without BOM, lowercase canonical UUID-only lines, and contain no duplicates. Exact-byte SHA-256 values match `run-result.json`: ActivityPriority ASC-1/ASC-2 `6f7f454fffab74103fb90f408dcf425440773c3a7b506717f4fc601ccf799c7f`, DESC `aff1f7df5f6ce29154e7a4e471524881492bc951f2219b50fceb6e7f9d523b36`; Lookup ASC-1/ASC-2 `6bde9135a2e13db9b25c0bd909e684df68c14a918347b159c88fd4528ab440fe`, DESC `e7cb12aad41916c1f7e21e8a2900ed1edc39ae7e01b2ade44a94da73a06bb33a`. Direct line-array comparisons reproduce ASC-1 = ASC-2 and `reverse(ASC-1)` = DESC for ActivityPriority (3 lines) and Lookup (109 lines). This confirms the bounded diagnostic sequences, not complete server population beyond the recorded traversal/terminal claims.
4. Page ledgers, terminal evidence, and their completeness limits. **Completed:** each persisted ledger independently reconciles with the matching IDs file. ActivityPriority: every pass has offsets `0,2`, row counts `2,1`, total `3`; Lookup: every pass has offsets `0,50,100`, row counts `50,50,9`, total `109`. First/last IDs in every ledger page match the corresponding slice of its IDs file; all final persisted pages are short. `ReadOrderedPassAsync` then issues a separate terminal query at offset equal to record count and throws if it is non-empty. Successful evidence generation source-traces that assertion, but no terminal response/shape or separate terminal ledger is persisted. Therefore the evidence proves the repeatable bounded, server-visible traversal asserted by the current source, while retaining the explicit limit that it cannot independently demonstrate an unrecorded population total or generalise to the full catalog/other schemas.
5. Response shapes, four schema mappings, registry relation, and Account duplicate-name impact. **Completed:** query response-shapes exactly allow `Id` only for ActivityPriority and `Id` plus `SysEntitySchemaUId` for Lookup, with `notFoundColumns` shape length `0`; no row values are present. Each schema response-shape has only `schema`, `success`, `errorInfo` at root and records `id`/`uId` as strings. The four mapping artefacts reconcile requested workspace UId to `schema.uId`: ActivityPriority/Base (9 columns: 1 own, 8 inherited, 2 reference mappings); Lookup/Base (12: 6 own, 6 inherited, 3 references); Account/Base (34: 28 own, 6 inherited, 16 references); Account/Completeness (35: 1 own, 34 inherited, 16 references). All selected schema mapping `IndexCount` values are zero. Registry artefact independently cross-checks: ActivityPriority schema UId equals its `SysEntitySchemaUId`; its registry `LookupRecordId` occurs exactly once in each 109-ID Lookup pass; its `RecordIds` equal ActivityPriority ASC-1; representation is `scalar-string`; base is BaseLookup. The duplicate `Account` schema name is material: Base and Completeness have different package names, schema UIds, and `SysSchemaIdCandidate` values. The current bounded source disambiguates these samples by package/UId, but a name-only full-catalog contract would be unsafe and is not proven by this sample.
6. `SysSchemaIdCandidate` and empty-index limitations. **Completed:** source reads `GetSchema.schema.id` as a UUID, persists it under the deliberately qualified label `SysSchemaIdCandidate`, and never equates it to a documented `SysSchemaId` contract. The four mapping files consistently cite source path `schema.id`, but this is field-shape/value evidence only; exact BPMSoft 1.8 semantic identity remains unverified. Each selected schema has `IndexCount=0`, and every schema response-shape has `indexes` array length `0`. Consequently this evidence validates presence of the indexes collection and the zero-index condition for these four bounded samples, not non-empty index representation, index metadata mapping, or write semantics.
7. Redaction, static endpoint boundary, not-run claims, final gate decision. **Completed (finding recorded; final matrix/verdict follows):** `redaction-check.json` says 23 files scanned and zero forbidden hits; the actual file count is 23. Independent case-insensitive scan found zero `UserPassword`, `Set-Cookie`, `BPMCSRF`, `Authorization`, bearer/basic/API-key, password/cookie/CSRF, `displayValue`, or `primaryImageValue` strings; recursive JSON value-only scan found zero sensitive values. `sourceCode`, `caption`, `description`, and `session` occur only as response-shape/property labels, not persisted values. There are no `*.response.json` files. Static source endpoint literals are Login, GetWorkspaceItems, GetSchema, and SelectQuery; no Insert/Update/Delete/Save/CanManage/CanWrite or Excel token/call was found. `run-result.json` says full catalog, write/manage, and Excel were not run; source success flow and evidence inventory are consistent with that bounded result.

No conclusion is valid until this file is updated to `COMPLETE`.

## Group 1 — exact-byte manifest

Command: `Get-ChildItem ... | Get-FileHash -Algorithm SHA256`, plus strict UTF-8/BOM and JSON parse checks in-memory.

| File | Bytes | SHA-256 |
|---|---:|---|
| ActivityPriority.asc-pass-1.ids.txt | 110 | `6f7f454fffab74103fb90f408dcf425440773c3a7b506717f4fc601ccf799c7f` |
| ActivityPriority.asc-pass-2.ids.txt | 110 | `6f7f454fffab74103fb90f408dcf425440773c3a7b506717f4fc601ccf799c7f` |
| ActivityPriority.desc-control.ids.txt | 110 | `aff1f7df5f6ce29154e7a4e471524881492bc951f2219b50fceb6e7f9d523b36` |
| lookup-ActivityPriority-ordered.response-shape.json | 350 | `3647e0f78329412b1679aee2e4f14b1a2e4fafe90b27bc3b3b0ea1b0185bf0d8` |
| lookup-Lookup-ordered.response-shape.json | 460 | `fa3ffbf25e8d5711e9a53d47da2050ff4f3a7372e92c8f938493155dc643a3cd` |
| lookup-registry-ActivityPriority.mapping.json | 759 | `db0afd574cdb6ce6f3083fb273e235152933b2df016fe9e1c1d49675021abf7b` |
| Lookup.asc-pass-1.ids.txt | 4032 | `6bde9135a2e13db9b25c0bd909e684df68c14a918347b159c88fd4528ab440fe` |
| Lookup.asc-pass-2.ids.txt | 4032 | `6bde9135a2e13db9b25c0bd909e684df68c14a918347b159c88fd4528ab440fe` |
| Lookup.desc-control.ids.txt | 4032 | `e7cb12aad41916c1f7e21e8a2900ed1edc39ae7e01b2ade44a94da73a06bb33a` |
| pagination-ActivityPriority-proof.json | 2034 | `887e66d5a3fe2a81619d6c66da1605f9c0d967765550567dc25a6344cdae3af2` |
| pagination-Lookup-proof.json | 7445 | `97cae1e2ded47ba672513ea48b7f06acf6df68903a477b1646532c5d07b5be44` |
| redaction-check.json | 257 | `2d82688044351d99e6eaf8deec2674338ca09f988a68aacba761331ab08f8114` |
| request-contract.json | 3312 | `f572d1cdaa68f5592ae31399def05aec3321b7db48390114286298fe453bd1be` |
| run-result.json | 1613 | `caa401c3b5185374fbf64bafabefbb78202e5aa99012485bb0086029901d05a0` |
| schema-Account-Base.mapping.json | 10853 | `24a13b0f54da6d20444c814d6cd54054322505f9c415e6e0560786885a90d19f` |
| schema-Account-Base.response-shape.json | 23171 | `b561eef7d1ca37573e4d685750fc5b70927c6cbfd24ee6977d7d795514ff188c` |
| schema-Account-extension.mapping.json | 11307 | `312b19aaba95dd3766b17ba5049d4c957a61483ac23814bfc2e2d35aaf2ce625` |
| schema-Account-extension.response-shape.json | 21418 | `b22539eca162aa7a6407b2e3fc9b72c77e40fdc2cd7803524c98ac48aad34692` |
| schema-ActivityPriority.mapping.json | 3354 | `6885b1609e66135dccfaaf19cd1de2feb6db2a9501660c7afba3ed5d7b0bdaef` |
| schema-ActivityPriority.response-shape.json | 16321 | `990e4b2a766ac6c1c5355257a42c7f0754745f3f05dc08dc05d1fbb4e9683d0c` |
| schema-Lookup.mapping.json | 4232 | `25329cd0368b0aff578aa74ae5787d60022ca91a26f1ef79770513ffc6142357` |
| schema-Lookup.response-shape.json | 16321 | `baa9ee33384a78dfef521865d695950704ad32c156e009c7f97e28db78764de1` |
| summary.json | 1623 | `3d2a76399bfd31d8bdd41d7c770c740207a1a413e8f9bf1eb8bc4205344ef2d4` |

## Completed offline commands

- `Get-Content -Raw docs\READ_ONLY_RESEARCH_PROMPTS\02-evidence-reviewer.md`
- `Get-ChildItem <evidence-root> -File | Sort-Object Name`, exact-byte `Get-FileHash -Algorithm SHA256`, strict UTF-8/no-BOM checks, and in-memory JSON parsing.
- `Get-FileHash` plus `FileVersionInfo`/`AssemblyName` inspection of the two assembly paths recorded in `request-contract.json`.
- Read-only reflection and method-body IL inspection for the recorded enum/types/member tokens; no type instantiation and no HTTP activity.
- In-memory `ReadAllLines`, canonical UUID validation, duplicate tests, SHA checks, direct ASC-1/ASC-2 and `reverse(ASC-1)`/DESC comparisons, and page-ledger reconciliation.
- Read-only JSON structural/mapping/registry checks; marker scans over all 23 files; static source searches for endpoint and write-operation literals/call sites.

No BPMSoft/login request was run. No source, prompt, contract, SDD/handoff, workbook, or `probe-output` file was changed. This Markdown report is the only file modified.

## Review matrix

| Claim | Status | Evidence path | Gap | Impact |
|---|---|---|---|---|
| Evidence set is a well-formed 23-file snapshot | Confirmed at review time | Evidence root; Group 1 manifest | Hashes establish current bytes, not pre-review provenance or future immutability. | Reproducible offline review baseline. |
| `allColumns=false` permits exactly the documented explicit-column payload | Confirmed for the hashed local assemblies | `request-contract.json`; hashed BPMSoft DLLs; `Program.cs` | Does not itself prove a live server response. | Removes the prior unsupported `allColumns=true`/columns explanation for this bounded payload. |
| `OrderDirection` wire values 1/2 and request shape are assembly-backed | Confirmed for the two pinned assemblies | `request-contract.json`; IL/token checks recorded in Group 2 | Only the supplied assembly versions are covered. | Explicit ordering hypothesis has a local binary basis. |
| ActivityPriority sequences are repeatable and DESC is exact reverse | Confirmed for the bounded 3-ID traversal | Three `ActivityPriority.*.ids.txt`; pagination proof | No independent persisted terminal response. | Supports this representative query/pass evidence. |
| Lookup sequences are repeatable and DESC is exact reverse | Confirmed for the bounded 109-ID traversal | Three `Lookup.*.ids.txt`; pagination proof | No independent persisted terminal response. | Supports this representative query/pass evidence. |
| Page ledgers are consistent with the saved sequences | Confirmed | `pagination-ActivityPriority-proof.json`; `pagination-Lookup-proof.json` | Ledger first/last IDs and short terminal page do not independently expose the terminal request/response. | Source-backed evidence of offsets and recorded page coverage. |
| Complete pagination/no skips is established for all relevant scope | Not confirmed | Same pagination proof; `ReadOrderedPassAsync` | No persisted terminal response or population total; only two bounded schemas are traversed. | Full-catalog completeness is not established. |
| Four schema mappings are exact enough for the whole contract | Partial | Four `schema-*.mapping.json`; response-shapes | Four bounded samples prove field paths and UId correspondence, not all entity/lookup variants or semantic identity of `schema.id`. | Mapping gate remains incomplete. |
| ActivityPriority-to-Lookup registry relation is reproducible | Confirmed for this one relation | `lookup-registry-ActivityPriority.mapping.json`; Lookup IDs; ActivityPriority mapping | No registry-wide mapping proof for all lookup schemas. | One exact `LookupRecordId`/`SysEntitySchemaUId` relation is demonstrated. |
| Schema name is a safe global identity | Contradicted | Account Base and extension mappings; `summary.json` | Both are named `Account` but differ in package and UIds. | Any later catalog logic must retain package/UId disambiguation; name-only matching is unsafe. |
| `SysSchemaIdCandidate` means the required `SysSchemaId` | Not confirmed | Four mappings; `Program.cs` `CreateSchemaMapping` | It is only the UUID value at `GetSchema.schema.id`, deliberately labelled candidate. | Do not promote it to an exact contract mapping. |
| Index mapping is proven | Not confirmed | Four mappings and schema response-shapes | All four have empty `indexes`; no non-empty-index structure/mapping was observed. | Index-dependent contract claims remain unsupported. |
| Evidence is redacted and source is bounded to read/login endpoints | Confirmed for saved evidence and static source intent | `redaction-check.json`; 23-file scans; `Program.cs` | Static inspection is not an HTTP audit trail. | No observed credential/session/raw row/value leakage; no write/Manage/Excel action evidenced. |
| Full catalog, write/Manage, and Excel were not run | Confirmed | `run-result.json`; directory inventory; source flow | None. | These scopes remain untested and unauthorized by this evidence. |

## Findings and limits

The retry rectifies the earlier failed direction control for precisely two server-visible representative traversals: the independently verified, pinned assembly metadata supports the explicit `Id` ordering payload, and all six persisted sequences satisfy the two ASC and exact-reverse DESC rules. The saved ledgers reconcile to their ID sequences and show offsets through the short final page.

This is not a proof of the entire future catalog or of every metadata field required by the approved contract. In particular, no raw terminal confirmation was retained, no population total is independently available, schema/registry coverage is bounded, `GetSchema.schema.id` remains a candidate rather than an accepted semantic `SysSchemaId`, and every observed index collection is empty. The duplicate `Account` schemas additionally rule out a global name-only identity.

## Explicitly not run

- Full-catalog traversal or scale measurement.
- Any write, Manage/permission, create/update/delete/save, compile, or Excel action.
- Any BPMSoft/login request by this reviewer.
- Any modification of the immutable evidence directory or project sources/specification artefacts.

## Owner question

Will you explicitly confirm that downstream implementation, full-catalog pull, `.xlsx`, and write/Manage scope remain stopped until the unresolved semantic `schema.id`/`SysSchemaId` mapping and non-empty-index representation are evidenced or removed from the required contract?

## Verdict

**G2 stop — DO NOT START.** Owner decision is required.
