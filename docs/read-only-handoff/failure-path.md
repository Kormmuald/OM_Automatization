# Failure path

For a named blocker, retain only safe reason, scope, recovery and next action in a new append-only root. On `TARGET_STATE_CHANGED_DURING_QUALIFICATION`, seal the terminal evidence, do not run Pass C or retry, and require a new manual invocation or direct current user request before a new run.
