# Offline configuration schema

Only a target alias, declared read scope, sanitized fixture path and optional plan hash are permitted. Do not put credentials, cookies, CSRF values, endpoint bodies or raw lookup values in configuration.
